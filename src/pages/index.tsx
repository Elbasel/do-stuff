import React from 'react';
import TextArea from '../components/InputContainer';

type Props = {};

const Home = (props: Props) => {
  return (
    <>
      <TextArea />
    </>
  );
};

export default Home;

import React from 'react';
import InputContainer from '../components/InputContainer';

type Props = {};

const Home = (props: Props) => {
  return (
    <>
      <InputContainer />
    </>
  );
};

export default Home;
